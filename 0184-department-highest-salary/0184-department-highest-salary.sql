select d.name as Department, e.name as Employee, e.salary as Salary from employee as e join department as d on d.id=e.Departmentid where e.salary=(select max(salary) from employee where d.id =departmentid);